services:
  postgres:
    image: postgres:16
    container_name: apex-postgres
    environment:
      POSTGRES_DB: apex
      POSTGRES_USER: apex
      POSTGRES_PASSWORD: apex
    ports:
      - "5432:5432"
    volumes:
      - apex-pgdata:/var/lib/postgresql/data

volumes:
  apex-pgdata:
